# ============================================================
# requirements.txt - Dependencias del asistente Miku
# Python 3.10+ sobre Windows.
#
# Solo figuran las librerías que se IMPORTAN/usarán en el código activo.
# Se ELIMINARON deps pesadas no usadas (torch, rvc-python, kokoro,
# chromadb, sentence-transformers, numpy, soundfile).
# IMPORTANTE: PyQt5, pyttsx3, pygame, pywin32 y demás
# se importan de forma LAZY (dentro de funciones) para no romper el
# arranque en modo texto.
# ============================================================

# ---------- Núcleo del sistema ----------
# HTTP (cerebro Groq por API y verificación del servidor VOICEVOX).
requests>=2.31.0

# ---------- Reconocimiento de voz (STT) ----------
# Motor de captura/transcripción de audio local.
SpeechRecognition>=3.10.0
# pyaudio lo importa SpeechRecognition en tiempo de ejecución para abrir
# el micrófono (requisito indirecto).
pyaudio>=0.2.13
# ---------- Voz de Miku (TTS con VOICEVOX) ----------
# Reproduce el WAV generado por VOICEVOX.
pygame>=2.5.0
# Fallback de voz del sistema cuando VOICEVOX no está corriendo.
pyttsx3>=2.90
# La traducción español -> japonés (para VOICEVOX) se hace con la API de Groq
# (cuenta de STT), reutilizando `requests`. Ya NO se usa deep-translator.

# ---------- Subtítulos en pantalla (overlay) ----------
# Ventana transparente "siempre encima" (solo en modo voz-push).
PyQt5>=5.15.10

# ---------- Control del sistema (plugins) ----------
# Abrir programas por nombre (usado por el plugin SystemControl).
AppOpener>=1.7
# Control de ventanas nativo de Windows (win32gui/con/api) y copiado al
# portapapeles del traductor de juegos (win32clipboard).
pywin32>=306
# Control de brillo de la pantalla (plugin SystemControl).
screen-brightness-control>=0.21.0
# Teclado global (push-to-talk F22 en main).
keyboard>=0.13.5
# Control de volumen y MUTE real (no toggle) en el Sistema: lo usa
# plugins/system_control.py vía IAudioEndpointVolume de pycaw.
# El código usa ``AudioUtilities.GetSpeakers().EndpointVolume`` (API de pycaw
# 2024+); con versiones más viejas el volumen quedaba inoperante en silencio.
pycaw>=20240210
comtypes>=1.1.12

# ---------- Estado del sistema y capturas ----------
# Estado del sistema (CPU/RAM/disco/batería) para plugins/system_status.py.
psutil>=5.9.0
# Captura de pantalla (plugins/captura.py) vía PIL.ImageGrab.
Pillow>=10.0.0

# ---------- OCR (opcional) ----------
# OCR de pantalla (plugins/ocr.py). OPCIONAL: si falta, se usa el OCR nativo
# de Windows (Windows.Media.Ocr). Requiere además el binario de Tesseract
# instalado en el sistema (con el paquete de idioma "spa" para español).
# pytesseract>=0.3.10

# ---------- Memoria semántica (opcional) ----------
# Embeddings livianos (ONNX, sin torch) para memoria/búsqueda semántica.
# OPCIONAL: si falta, la memoria usa LIKE y la búsqueda de archivos usa su
# scoring normal. Suma onnxruntime (~100-200 MB en disco).
# fastembed>=0.3.0

# ---------- Telegram (opcional) ----------
# Control remoto por Telegram (plugins/telegram_control.py). OPCIONAL: si
# falta, el plugin queda inactivo.
# python-telegram-bot>=21.0

# ---------- Discord (bot de moderación) ----------
# Corre en su propio hilo/loop async (plugins/discord_control.py). Se importa
# de forma LAZY: si falta, el plugin queda inactivo y el asistente igual arranca.
discord.py>=2.3.2

# ---------- Opcionales (comentados) ----------
# TTS online alternativo (si preferís edge-tts a VOICEVOX):
# edge-tts>=6.1.10
# Lectura de variables de entorno desde archivo .env:
# python-dotenv>=1.0.0


# ---------- TIDAL por nombre (opcional) ----------
# "Poné Bohemian Rhapsody" (plugins/multimedia/tidal.py): busca el tema en TU cuenta de TIDAL.
# Sin esto, TIDAL solo se controla (pausa/siguiente/qué suena). Luego decí "conectá TIDAL".
# tidalapi>=0.8.0

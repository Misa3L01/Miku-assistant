# requirements.txt - dependencias de Miku Assistant (Python 3.10+ en Windows).
#
# Todas se importan de forma perezosa: si falta una, la función que la usa se desactiva
# (Miku lo avisa en el log) y el resto sigue andando. Para ver qué te falta:
#   python -m miku.ajustes estado

# ---------- Núcleo ----------
requests>=2.31.0                  # LLM y Whisper (Groq), traducción, VOICEVOX, Telegram, Todoist
psutil>=5.9.0                     # CPU / RAM / disco / batería / procesos
Pillow>=10.0.0                    # capturas de pantalla y visión

# ---------- Voz ----------
SpeechRecognition>=3.10.0         # captura del micrófono (la transcripción la hace Groq o faster-whisper)
pyaudio>=0.2.13                   # lo usa SpeechRecognition para abrir el micrófono
pygame>=2.5.0                     # reproduce el audio de VOICEVOX
pyttsx3>=2.90                     # voz del sistema: respaldo cuando VOICEVOX no responde

# ---------- Interfaz ----------
PyQt5>=5.15.10                    # bandeja, subtítulos y ventana de depuración

# ---------- Control de Windows ----------
pywin32>=306                      # ventanas, procesos, portapapeles
keyboard>=0.13.5                  # tecla de invocación global (F13)
AppOpener>=1.7                    # abrir programas por nombre
screen-brightness-control>=0.21.0 # brillo de la pantalla
pycaw>=20240210                   # volumen y silencio reales (API de pycaw 2024+)
comtypes>=1.1.12                  # requerido por pycaw
keyring>=25.0                     # contraseña del comedor en el Administrador de credenciales

# ---------- Servicios ----------
discord.py>=2.3.2                 # bot de moderación de Discord (plugin opcional)
websocket-client>=1.6.0           # Brave, TIDAL, comedor y WhatsApp por el protocolo de depuración (CDP)

# ---------- Opcionales (descomentá lo que uses) ----------
# python-telegram-bot>=21.0       # control remoto por Telegram
# tidalapi>=0.8.11                # "poné <canción>" en TIDAL (busca en tu cuenta)
# fastembed>=0.3.0                # memoria por significado (embeddings ONNX, sin torch)
# faster-whisper>=1.0.0           # STT_PROVEEDOR/WAKE_PROVEEDOR = "local": transcribir sin nube
# openwakeword>=0.6.0             # WAKE_PROVEEDOR = "openwakeword" (necesita un modelo .onnx de "Miku")
#
# Fin de frase con silero-vad: NO hace falta instalar nada (el modelo se baja con
#   python -m miku.voz.entrada.vad descargar
# y corre sobre onnxruntime). El paquete oficial arrastra torch, por eso no se usa:
# silero-vad>=5.1                 # solo si preferís el paquete oficial (instala torch, ~2 GB)
# pytesseract>=0.3.10             # OCR alternativo (necesita el binario de Tesseract con idioma "spa")
# win10toast>=0.9                 # notificaciones nativas (sin esto se usa PowerShell)
